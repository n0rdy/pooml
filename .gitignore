.idea
*.iml
.DS_Store
.claude
dist

CONTEXT.md
ROADMAP.md
# internal deep-dives split out of CONTEXT.md; same status as it
docs/

test_data_large.sql

# Astro build outputs live in site/dist and site/node_modules,
# covered by site/.gitignore. site/public/ is SOURCE (static assets,
# CNAME) and must be committed - the Hugo convention from forq, where
# public/ is output, does not apply here.
.hugo_build.lock

# Node.js dependencies  
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Hugo Modules: go.mod and go.sum should be tracked
# package-lock.json should be tracked for reproducible builds

# Logs
*.log
*/**.log
# Added by goreleaser init:
dist/

CLAUDE.md
# frontend asset toolchain (outputs in ui/static are committed)
ui/assets/node_modules/
